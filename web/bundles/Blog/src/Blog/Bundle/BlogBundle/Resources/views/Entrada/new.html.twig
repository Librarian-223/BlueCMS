<h1>Entrada creation</h1>

<form action="{{ path('manage_entrada_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('manage_entrada') }}">
            Back to the list
        </a>
    </li>
</ul>
